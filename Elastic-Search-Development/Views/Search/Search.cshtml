@model IEnumerable<Elastic_Search_Development.Models.Customer>

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <!--<form>
        <div class="form-row">
            <div class="form-group col-md-3">
                <label for="txtCustomer">Customer</label>
                <input type="text" class="form-control" id="txtCustomer" placeholder="Enter A Customer">
            </div>
        </div>
        <br />
        <button type="button" id="btnsearch" class="btn btn-primary">Search</button>
        <button type="button" id="btnclear" class="btn btn-info">Clear</button>
    </form>*/
    <br />-->

    <!-- Another variation with a button -->
    <div class="input-group searchContainer">
        <input type="text" class="form-control" id="txtCustomer" placeholder="Enter A Customer">
        <div class="input-group-append">
            <button id="btnsearch" class="btn btn-primary" type="button">
                <i class="fa fa-search"></i>
            </button>
        </div>
    </div>

    <table id="theTable" class="table table-striped table-dark">
        <thead>
            <tr>
                <th scope="col">Customer Id</th>
                <th scope="col">First Name</th>
                <th scope="col">Last Name</th>
                <th scope="col">Date Of Birth</th>
                <th scope="col">SSN</th>
                <th scope="col">Email</th>
                <th scope="col">Address1</th>
                <th scope="col">City</th>
                <th scope="col">State Province</th>
                <th scope="col">Postal Code</th>
                <th scope="col">Country</th>
                <th scope="col">Phone1</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Any())
            {
                foreach (var customer in Model)
                {
                    <tr>
                        <td>@customer.CustomerId</td>
                        <td>@customer.FirstName</td>
                        <td>@customer.LastName</td>
                        <td>@customer.DateOfBirth</td>
                        <td>@customer.SSN</td>
                        <td>@customer.Email</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td>No Results Found</td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>
